# 皮肤模板区域切分总结

## 区域位置信息（裁剪坐标）

### 画布总尺寸

- **整张模板尺寸**：`4096 × 2048` px（推测，依据「4K_Skin_Template」命名 + 各区块尺寸拼接）
- **坐标系**：原点 `(0, 0)` 位于左上角，X 轴向右，Y 轴向下
- **坐标格式**：`(x, y, w, h)` —— 左上角坐标 + 宽高

### 各区块坐标一览

| 编号 | x | y | w | h | 右下角 (x₂, y₂) | 用途 |
| --- | --- | --- | --- | --- | --- | --- |
| A | 0 | 0 | 1536 | 1536 | (1536, 1536) | 头部 Base |
| B | 1536 | 0 | 1536 | 1536 | (3072, 1536) | 头部 Outline |
| C | 3072 | 0 | 512 | 512 | (3584, 512) | 手 Base |
| D | 3584 | 0 | 512 | 512 | (4096, 512) | 手 Outline |
| E | 3072 | 512 | 1024 | 512 | (4096, 1024) | 脚 Base |
| F | 3072 | 1024 | 1024 | 512 | (4096, 1536) | 脚 Outline |
| G1 | 1024 | 1536 | 512 | 512 | (1024, 2048) | 普通眼 |
| G2 | 1536 | 1536 | 512 | 512 | (1536, 2048) | 生气眼 |
| G3 | 2048 | 1536 | 512 | 512 | (2048, 2048) | 笨拙眼 |
| G4 | 2560 | 1536 | 512 | 512 | (2560, 2048) | 快乐眼 |
| H | 3584 | 1536 | 512 | 512 | (4096, 2048) | 呆呆眼 |

![4K_Skin_Template.png](%E7%9A%AE%E8%82%A4%E6%A8%A1%E6%9D%BF%E5%8C%BA%E5%9F%9F%E5%88%87%E5%88%86%E6%80%BB%E7%BB%93/4K_Skin_Template.png)

---

## 六、参考代码（Python / Pillow）

### 方案 A：声明式字典 + 批量裁剪

```python
from PIL import Image
from pathlib import Path

# (x, y, w, h)
REGIONS = {
    "head_base":     (0,    0,    1536, 1536),  # A  头部 Base
    "head_outline":  (1536, 0,    1536, 1536),  # B  头部 Outline
    "hand_base":     (3072, 0,    512,  512),   # C  手 Base
    "hand_outline":  (3584, 0,    512,  512),   # D  手 Outline
    "foot_base":     (3072, 512,  1024, 512),   # E  脚 Base
    "foot_outline":  (3072, 1024, 1024, 512),   # F  脚 Outline
    "eye_normal":    (1024, 1536, 512,  512),   # G1 普通眼
    "eye_angry":     (1536, 1536, 512,  512),   # G2 生气眼
    "eye_clumsy":    (2048, 1536, 512,  512),   # G3 蠢拙眼
    "eye_happy":     (2560, 1536, 512,  512),   # G4 快乐眼
    "eye_dazed":     (3584, 1536, 512,  512),   # H  呆呆眼
}

def crop_regions(src: str, out_dir: str = "./out"):
    img = Image.open(src)
    Path(out_dir).mkdir(parents=True, exist_ok=True)
    for name, (x, y, w, h) in REGIONS.items():
        # Pillow 的 crop 接受 (left, upper, right, lower)
        tile = img.crop((x, y, x + w, y + h))
        tile.save(f"{out_dir}/{name}.png")
        print(f"saved {name}.png  {w}x{h} @ ({x},{y})")

if __name__ == "__main__":
    crop_regions("4K_Skin_Template.png")
```

### 方案 B：含元数据的结构化定义（推荐用于后续合成）

```python
from dataclasses import dataclass
from typing import Literal, Optional

@dataclass
class Region:
    name: str
    x: int
    y: int
    w: int
    h: int
    role: Literal["base", "outline", "detail"]
    pair: Optional[str] = None       # 对应的成对区块名
    semantic: Optional[str] = None   # 语义标签：head / limb / eye ...

REGIONS = [
    Region("A",  0,    0,    1536, 1536, "base",    pair="B",  semantic="head"),
    Region("B",  1536, 0,    1536, 1536, "outline", pair="A",  semantic="head"),
    Region("C",  3072, 0,    512,  512,  "base",    pair="D",  semantic="hand"),
    Region("D",  3584, 0,    512,  512,  "outline", pair="C",  semantic="hand"),
    Region("E",  3072, 512,  1024, 512,  "base",    pair="F",  semantic="foot"),
    Region("F",  3072, 1024, 1024, 512,  "outline", pair="E",  semantic="foot"),
    Region("G1", 1024, 1536, 512,  512,  "detail",  semantic="eye_normal"),
    Region("G2", 1536, 1536, 512,  512,  "detail",  semantic="eye_angry"),
    Region("G3", 2048, 1536, 512,  512,  "detail",  semantic="eye_clumsy"),
    Region("G4", 2560, 1536, 512,  512,  "detail",  semantic="eye_happy"),
    Region("H",  3584, 1536, 512,  512,  "detail",  semantic="eye_dazed"),
]
```

### 方案 C：JSON 配置（便于跨语言读取）

```json
{
  "canvas": { "w": 4096, "h": 2048 },
  "regions": [
    { "id": "A",  "x": 0,    "y": 0,    "w": 1536, "h": 1536, "role": "base",    "semantic": "head" },
    { "id": "B",  "x": 1536, "y": 0,    "w": 1536, "h": 1536, "role": "outline", "semantic": "head" },
    { "id": "C",  "x": 3072, "y": 0,    "w": 512,  "h": 512,  "role": "base",    "semantic": "hand" },
    { "id": "D",  "x": 3584, "y": 0,    "w": 512,  "h": 512,  "role": "outline", "semantic": "hand" },
    { "id": "E",  "x": 3072, "y": 512,  "w": 1024, "h": 512,  "role": "base",    "semantic": "foot" },
    { "id": "F",  "x": 3072, "y": 1024, "w": 1024, "h": 512,  "role": "outline", "semantic": "foot" },
    { "id": "G1", "x": 1024, "y": 1536, "w": 512,  "h": 512,  "role": "detail",  "semantic": "eye_normal" },
    { "id": "G2", "x": 1536, "y": 1536, "w": 512,  "h": 512,  "role": "detail",  "semantic": "eye_angry" },
    { "id": "G3", "x": 2048, "y": 1536, "w": 512,  "h": 512,  "role": "detail",  "semantic": "eye_clumsy" },
    { "id": "G4", "x": 2560, "y": 1536, "w": 512,  "h": 512,  "role": "detail",  "semantic": "eye_happy" },
    { "id": "H",  "x": 3584, "y": 1536, "w": 512,  "h": 512,  "role": "detail",  "semantic": "eye_dazed" }
  ]
}
```

---

## 七、坐标校验小贴士

1. **先读取图片真实尺寸** 再裁剪：
    
    ```python
    from PIL import Image
    img = Image.open("4K_Skin_Template.png")
    print(img.size)  # 期望 (4096, 2048)
    ```
    
    如果实际尺寸不是 `4096 × 2048`，按比例缩放坐标即可：`scale = real_w / 4096`。
    
2. **成对配准**：A↔B、C↔D、E↔F 形状理论上完全对齐，可直接将黑色块作为 Alpha 通道叠到彩色块上：
    
    ```python
    from PIL import Image, ImageChops
    base    = Image.open("head_base.png").convert("RGBA")
    outline = Image.open("head_outline.png").convert("L")  # 黑→暗
    # 黑色作为描边层 Multiply
    merged = ImageChops.multiply(base, Image.merge("RGBA", (outline,)*3 + (Image.new("L", base.size, 255),)))
    ```
    

---